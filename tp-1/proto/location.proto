syntax = "proto3";

package location;

import "common.proto";

// Solicitud con una IP para resolver su localización
message LocationRequest {
  string ip = 1;
}

service LocationService {
  rpc GetLocation(LocationRequest) returns (common.Location);
}

