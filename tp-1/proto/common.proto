syntax = "proto3";

package common;

// Shared Location message used by both location and weather services
message Location {
  string country = 1;
  string city = 2;
  double latitude = 3;
  double longitude = 4;
  string timezone = 5;  // Timezone ID (e.g., "America/Los_Angeles")
}
